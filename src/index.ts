import {createEditorState} from './editor-state'
import {createEditorView,destroyEditorView} from './editor-view'
import {EditorTools as getEditorTools} from './editor-tools'

export {
  createEditorState,
  createEditorView,
  destroyEditorView,
  getEditorTools
}